#include<stdio.h>
void main()
{
    float pi,radius = 1.0,area = 1.0;
   // int radius;
    pi =22.0/(7.0);
    printf("enter the radius");
    scanf("%f",&radius);
    area = pi *(radius * radius);
    printf("%f",area);

} 